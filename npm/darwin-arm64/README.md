# snapgrep-darwin-arm64

Prebuilt native addon for `snapgrep` on darwin arm64.

Installed automatically as an optional dependency of [snapgrep](https://github.com/Owen718/snapgrep). Nothing here is meant to be imported directly.
